// import { selectPatientsPageDomain } from '../selectors';

describe('selectPatientsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
